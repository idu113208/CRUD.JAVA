## 2024-05-23 - Authentication Forms UX
**Learning:** Legacy PHP forms often lack basic accessibility attributes (`for`/`id`) and `autocomplete`, which are critical for password managers.
**Action:** Always check `views/auth/` for these attributes and ensure navigation between Login/Register exists to prevent dead ends.
