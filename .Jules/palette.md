# Palette's Journal

## 2024-05-22 - Accessibility in Legacy Forms
**Learning:** Legacy PHP forms often lack explicit label-input associations (`for`/`id`), which hurts screen reader accessibility.
**Action:** Always verify `for` and `id` attributes match when touching form files, even if they look "fine" visually.
